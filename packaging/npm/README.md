# simplicio-loop

Thin npm wrapper for the `simplicio-loop` package on PyPI.

On first run it creates a versioned virtualenv under the user's cache directory,
installs `simplicio-loop==3.15.0`, and forwards all arguments to the Python CLI.
